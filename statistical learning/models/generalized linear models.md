[[linear models]]

$g(\mathbb{E}\left[Y \mid X\right]) = \alpha + \sum\limits_{i=1}\beta_iX_i$ 

$$
\underbrace{
g(\mathbb{E}\left[Y \mid X\right]) 
}_\text{linking function}
=
\underbrace{
\alpha
}_\text{indipendent coefficient}
+ 
\sum\limits^{M}_{i=1}
\underbrace{
\beta_i
}_\text{coefficient}
\overbrace{
X_i
}^\text{one input feature})
$$

- link function $g$ often logit or identity

# training with backfitting algorithm

START
Basic
generalized linear models
- definition
- and name of training algorithm
Back: 
$$
\underbrace{
g(\mathbb{E}\left[Y \mid X\right]) 
}_\text{linking function}
=
\underbrace{
\alpha
}_\text{indipendent coefficient}
+ 
\sum\limits^{M}_{i=1}
\underbrace{
\beta_i
}_\text{coefficient}
\overbrace{
X_i
}^\text{one input feature})
$$
- link function $g$ often logit or identity
training: with backfitting algorithm
Tags: statistical learning, models
<!--ID: 1663839912713-->
END