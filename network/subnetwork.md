- Dividing an [[internet protocol (IP)]] network into two or more networks
logical division of an IP address into two fields: the _network number_ or _routing prefix_, and the _rest field_ or _host identifier_

The _rest field_ is an identifier for a specific [host](https://en.wikipedia.org/wiki/Host_(network) "Host (network)") or network interface.
![[subnet.png]]