### DNS hostname
- in [[AWS]] each [[internet protocol (IP)]] adress has halso a [[domain name system (DNS)]] name that is linking to it
- [[AWS private ip]] adresses have a internat dns hostname and [[AWS public ip]] adresses have a public dnc hostname
- an be of two types: resource-based or IP-based