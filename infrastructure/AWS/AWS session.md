### AWS session
- a [[AWS session]] is a interaction with [[AWS]]  for a temporary period during with temporary credentials 
- initiated with [[AWS STS (security token service)]] or [[AWS sso (single sign on)]]


