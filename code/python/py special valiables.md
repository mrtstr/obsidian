- Valiables set by and used by the [[py interpreter]]
`__name__` current [[py module]] name or `__main__` when the file is called directly
`__file__` path of the [[python]] file
`__path__` path of the folder of the [[py package]] (if it's not defined the [[py module]] is not a [[py package]])

# Anki
START
Basic
[[py special valiables]]
- definition
- 3 examples and what they do
Back: 
- Valiables set by and used by the [[py interpreter]]
`__name__` current [[py module]] name or `__main__` when the file is called directly
`__file__` path of the [[python]] file
`__path__` path of the folder of the [[py package]] (if it's not defined the [[py module]] is not a [[py package]])

Tags: code python
<!--ID: 1698674406761-->
END

START
Basic
what are the valiables `__name__` , `__file__` and `__path__` 
Back: 
- Valiables set by and used by the [[py interpreter]]
`__name__` current [[py module]] name or `__main__` when the file is called directly
`__file__` path of the [[python]] file
`__path__` path of the folder of the [[py package]] (if it's not defined the [[py module]] is not a [[py package]])
Tags: code python
<!--ID: 1698674406766-->
END