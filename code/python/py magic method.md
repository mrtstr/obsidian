2) `__get_item__()`: used for [[py list]] indexing, [[py dict]] lookup, or accessing ranges of values. Is called whenever a value in square backets is passed1) `__iter__()`: Usualy called by the [[py buidin functions|buidin function inter()]] to convert a [[py interatable]] into an [[py iterator]]. If the object is already an [[py iterator]] it returns itself.
2) `__next__()`: computes and returns the next value in the squence

1) `__iter__()`: Usualy called by the [[py buidin functions|buidin function inter()]] to convert a [[py interatable]] into an [[py iterator]]. If the object is already an [[py iterator]] it returns itself.
2) `__next__()`: computes and returns the next value in the squence