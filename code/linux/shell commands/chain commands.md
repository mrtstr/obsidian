```
& (Ampersand)
This command sends a process/script/command to the background. 

&& (**Logical AND)
The command following this operator will only execute if the command preceding this operator has been successfully executed. 

; (Semi-colon)
The command following this operator will execute even if the command preceding this operator is not successfully executed.

|| ( Logical OR)
The command succeeding this operator is only executed if the command preceding it has failed.

| (Pipe)
The output of the first command acts as input to the second command.

! (NOT)
Negates an expression within a command. It is much like an “except” statement.

>,>>,  (Redirection)
Redirects the output of a command or a group of commands to a file or stream.

&&-|| (AND-OR)
It is a combination of AND OR operator and is similar to the if-else statement.

\ (Concatenation)
Used to concatenate large commands over several lines in the shell.

() (Precedence)
Allows command to execute in precedence order.

{} (Combination)
The execution of the command succeeding this operator depends on the execution of the first command.
```


START
Basic
How to run shell command sequentially in the background
 
Back: 
(spaces are important)
```
bash -c "{ <command1>; <command2>; } &"
```
Tags: shell
<!--ID: 1667556040457-->
END

START
Basic
shell operators
```
& 

&& 

; 

|| 

| (

! 

>,>>,  

&&-|| 

\ 

() 

{} 
```
Back: 
```
& (Ampersand)
This command sends a process/script/command to the background. 

&& (**Logical AND)
The command following this operator will only execute if the command preceding this operator has been successfully executed. 

; (Semi-colon)
The command following this operator will execute even if the command preceding this operator is not successfully executed.

|| ( Logical OR)
The command succeeding this operator is only executed if the command preceding it has failed.

| (Pipe)
The output of the first command acts as input to the second command.

! (NOT)
Negates an expression within a command. It is much like an “except” statement.

>,>>,  (Redirection)
Redirects the output of a command or a group of commands to a file or stream.

&&-|| (AND-OR)
It is a combination of AND OR operator and is similar to the if-else statement.

\ (Concatenation)
Used to concatenate large commands over several lines in the shell.

() (Precedence)
Allows command to execute in precedence order.

{} (Combination)
The execution of the command succeeding this operator depends on the execution of the first command.
```
Tags: shell
END


START
Basic
name the shell operator
```

This command sends a process/script/command to the background. 


The command following this operator will only execute if the command preceding this operator has been successfully executed. 


The command following this operator will execute even if the command preceding this operator is not successfully executed.


The command succeeding this operator is only executed if the command preceding it has failed.


The output of the first command acts as input to the second command.


Negates an expression within a command. It is much like an “except” statement.


Redirects the output of a command or a group of commands to a file or stream.


It is a combination of AND OR operator and is similar to the if-else statement.


Used to concatenate large commands over several lines in the shell.


Allows command to execute in precedence order.


The execution of the command succeeding this operator depends on the execution of the first command.
```
Back: 

```
& (Ampersand)
This command sends a process/script/command to the background. 

&& (**Logical AND)
The command following this operator will only execute if the command preceding this operator has been successfully executed. 

; (Semi-colon)
The command following this operator will execute even if the command preceding this operator is not successfully executed.

|| ( Logical OR)
The command succeeding this operator is only executed if the command preceding it has failed.

| (Pipe)
The output of the first command acts as input to the second command.

! (NOT)
Negates an expression within a command. It is much like an “except” statement.

>,>>,  (Redirection)
Redirects the output of a command or a group of commands to a file or stream.

&&-|| (AND-OR)
It is a combination of AND OR operator and is similar to the if-else statement.

\ (Concatenation)
Used to concatenate large commands over several lines in the shell.

() (Precedence)
Allows command to execute in precedence order.

{} (Combination)
The execution of the command succeeding this operator depends on the execution of the first command.
```
Tags: shell
<!--ID: 1667564027878-->
END