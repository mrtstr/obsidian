1) Replacing by value using [[pd replace]]: 
2) replacing by position ([[pd index]], [[pd column]])
	1) [[pd loc]], [[pd iloc]]:replacing scalars
	2) [[pd loc]], [[pd iloc]]: replacing a slice (can be unsave without copy)
	3) [[pd update]]: replacing with the elements from onother [[pd dataframe]], [[pd series]], [[py dict]] by allining [[pd index]] and [[pd column]]



# Anki
START
Basic
[[pandas]] [[pd replacing values in a dataframe]] 1 plus 3 methods
Back: 

1) Replacing by value using [[pd replace]]: 
2) replacing by position ([[pd index]], [[pd column]])
	1) [[pd loc]], [[pd iloc]]:replacing scalars
	2) [[pd loc]], [[pd iloc]]: replacing a slice (can be unsave without copy)
	3) [[pd update]]: replacing with the elements from onother [[pd dataframe]], [[pd series]], [[py dict]] by allining [[pd index]] and [[pd column]]


Tags: code pandas
<!--ID: 1698489556126-->
END