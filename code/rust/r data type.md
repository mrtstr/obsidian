### data type aliases types

- in [[rust]] [[r data type]] aliases can be declared using the keyword `type`

```rust
type Kilometers = i32;

let x: i32 = 5;
let y: Kilometers = 5;

println!("x + y = {}", x + y);
```


START
Basic
[[r data type]] aliases in [[rust]]

Back: 
### data type aliases types

- in [[rust]] [[r data type]] aliases can be declared using the keyword `type`

```rust
type Kilometers = i32;

let x: i32 = 5;
let y: Kilometers = 5;

println!("x + y = {}", x + y);
```

Tags: code rust
<!--ID: 1759683035828-->
END